#include <stdio.h>
int main() {
    int N, temp, rev = 0;
    scanf("%d", &N);
    temp = N;
    for (int i=0; N > 0; N = N / 10) {
        rev = rev * 10 + (N % 10);
    }

    if (temp == rev)
        printf("Yes");
    else
        printf("No");

    return 0;
}